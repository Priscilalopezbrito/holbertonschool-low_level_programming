#include "main.h"
/**
 * _abs - looks for lowercase character
 * @n: int to check
 *
 * Description: calculate abs
 *
 * Return: returns 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
		return (n);
}
