#include <stdio.h>
#include <stdlib.h>
#include "3-calc.h"

int main(int argc, char *argv[])
{
	int num1;
	int num2;

	num1 = atoi(argv[1]);
	num2 = atoi(arg[3]);
	if (arg != 4)
	{
		printf("Error");
		exit (98);
	}
	else
	{
		return ((get_op_func(argv[2])(num1, num2));
	}
}
