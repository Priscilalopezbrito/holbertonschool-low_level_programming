#include "main.h"
/**
 *mul - looks for lowercase character
 *@a: first number
 *@b: second number
 * Description: mul
 *
 * Return: returns 0
 */
int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
